<% if @task.save %>
  $('.js-tasks').prepend("<%= escape_javascript(render partial: 'tasks', locals: {tasks: @tasks}) %>");
<% else %>
  <('.new-task').html("<%= escape_javascript(render partial: 'tasks/form', locals {task: @new_task} %>");
<% end %>