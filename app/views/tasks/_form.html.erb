<%= form_for [@tasks] do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control', placeholder: 'Tell us what you need to do.' %>
  </div>
<% end %>