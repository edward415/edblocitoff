<tbody>
<% @tasks.each do |t| %>
    <tr>
      <td><%= t.name %></td>
      <td><%= distance_of_time_in_words_to_now(t.expires_at) %></td>
      <td><%= link_to "Completed", task_complete_path(t), method: :put %></td>
    </tr>
<% end %>  
</tbody>
