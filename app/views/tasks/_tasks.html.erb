<tbody>
<% @tasks.each do |t| %>
    <tr>
      <td><%= t.name %></td>
      <td><%= distance_of_time_in_words(Time.now, t.expires_at) %></td>
      <td><%= t.completed %></td>
    </tr>
<% end %>  
</tbody>